#!/bin/sh /etc/rc.common

START=99
USE_PROCD=1

start_service() {
        [[ ! -f /tmp/auto-hhu.log ]] && touch /tmp/auto-hhu.log
        procd_open_instance
        procd_set_param command /usr/sbin/auto-hhu.sh
        procd_set_param respawn
        procd_close_instance
}